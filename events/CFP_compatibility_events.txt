namespace = cfp_compatibility

cfp_compatibility.1 = { # Community Flavor Pack compatibility
    hidden = yes
	orphan = yes 
	trigger = {
		exists = global_var:cfp_is_loaded
	}
	immediate = {}
}

cfp_compatibility.2 = { # The Catholic Trinity compatibility
    hidden = yes
	orphan = yes 
	immediate = {
		set_global_variable = {
			name = Trinity_is_loaded
			value = yes
		}
	}
}
