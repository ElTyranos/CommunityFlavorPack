cfp_commission_concert_decision = {
	picture = "gfx/interface/illustrations/decisions/cfp_decision_moc.dds"
	
	sort_order = 69
	
	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { highest_held_title_tier >= tier_kingdom }
				desc = cfp_commission_concert_decision_has_court_desc
			}
			triggered_desc = {
				trigger = { highest_held_title_tier = tier_duchy }
				desc = cfp_commission_concert_decision_has_no_court_desc
			}
		}
	}
	selection_tooltip = {
		first_valid = {
			triggered_desc = {
				trigger = { highest_held_title_tier >= tier_kingdom }
				desc = cfp_commission_concert_decision_has_court_tooltip
			}
			triggered_desc = {
				trigger = { highest_held_title_tier = tier_duchy }
				desc = cfp_commission_concert_decision_has_no_court_tooltip
			}
		}
	}

	cooldown = { years = 2 }

	is_shown = {
		is_landed = yes
		OR = {
			AND = {
				highest_held_title_tier >= tier_kingdom
				employs_court_position = court_musician_court_position
			}
			AND = {
				is_independent_ruler = no
				highest_held_title_tier = tier_duchy
				liege = { employs_court_position = court_musician_court_position }
			}
		}
	}
	
	is_valid = {
		trigger_if = {
			limit = { highest_held_title_tier >= tier_kingdom }
			employs_court_position = court_musician_court_position
		}
		trigger_if = {
			limit = { highest_held_title_tier = tier_duchy }
			liege = { 
				employs_court_position = court_musician_court_position
				# opinion = {
					# target = root
					# value >= 20
				# }
			}
			
		}
	}

	is_valid_showing_failures_only = {
		is_alive = yes
		is_in_an_activity = no
		is_imprisoned = no
		has_contagious_deadly_disease_trigger = no
	}

	cost = {
		gold = {
			value = medium_gold_value
			if = {
				limit = { highest_held_title_tier = tier_duchy }
				multiply = 0.25
			}
		}
	}

	effect = {
		stress_impact = {
			greedy = minor_stress_impact_gain
			shy = minor_stress_impact_gain
			gregarious = minor_stress_impact_loss
		}
		if = {
			limit = { highest_held_title_tier >= tier_kingdom }
			add_prestige = minor_prestige_gain
			trigger_event = cfp_moc.0001
		}
		if = {
			limit = { highest_held_title_tier = tier_duchy }
			# add_prestige = minor_prestige_gain
			custom_tooltip = cfp_commission_concert_decision_desc_vassals_opinion
			custom_tooltip = cfp_commission_concert_decision_desc_liege_opinion_increase
			custom_tooltip = cfp_commission_concert_decision_desc_liege_opinion_refuse
			# trigger_event = cfp_moc.0001
		}
	}
	
	ai_check_interval = 120
	
	ai_potential = {
		short_term_gold >= major_gold_value
	}

	ai_will_do = {
		base = 100
	}
}
