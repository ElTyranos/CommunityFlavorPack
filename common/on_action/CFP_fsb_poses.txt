new_poses_on_action = {
	on_actions = {
		cfp
	}
}

cfp = {
	effect = {
		if = {
			limit = { exists = global_var:FSB_is_loaded }
			cfp_add_pose_to_list = { list = cfp pose = cfp_regalia }
			cfp_add_pose_to_list = { list = cfp pose = cfp_globus }
			cfp_add_pose_to_list = { list = cfp pose = cfp_scepter }
		}
		if = {
			limit = { 
				exists = global_var:FSB_is_loaded
				OR = {
					has_game_rule = barbershop_cfp_fantasy
					has_game_rule = ai_cfp_fantasy
				}
			}
			cfp_add_pose_to_list = { list = cfp pose = cfp_bonfire }
			cfp_add_pose_to_list = { list = cfp pose = cfp_broom }
			cfp_add_pose_to_list = { list = cfp pose = cfp_potion }
			cfp_add_pose_to_list = { list = cfp pose = cfp_cauldron }
		}
	}
}
